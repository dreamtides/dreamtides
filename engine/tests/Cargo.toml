[package]
name = "tests"
version = "0.0.0"
edition = "2021"

[dependencies]
ability_data = { path = "../src/ability_data" }
parser = { path = "../src/parser" }

[dev-dependencies]
insta = { version = "1.42", features = ["ron"] }
