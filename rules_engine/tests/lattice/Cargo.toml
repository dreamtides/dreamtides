[package]
name = "lattice_tests"
version = "0.0.0"
edition = "2024"

[lints]
workspace = true

[lib]
test = false
doctest = false
bench = false

[dependencies]
lattice = { path = "../../src/lattice" }

tempfile = { workspace = true }
