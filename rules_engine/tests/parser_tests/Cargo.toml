[package]
name = "parser_tests"
version = "0.0.0"
edition = "2021"

[lints]
workspace = true

[lib]
test = false
doctest = false
bench = false

[dependencies]
ability_data = { path = "../../src/ability_data" }
parser = { path = "../../src/parser" }

ariadne = "0.2"

[dev-dependencies]
insta = { workspace = true }
