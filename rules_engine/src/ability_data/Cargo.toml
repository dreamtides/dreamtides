[package]
name = "ability_data"
version = "0.0.0"
edition = "2021"
[lints]
workspace = true


[lib]
test = false
doctest = false
bench = false

[dependencies]
core_data = { path = "../core_data" }

schemars = "0.8"
serde = { version = "1", features = ["derive"] }
strum = "0.27"