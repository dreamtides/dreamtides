[package]
name = "parser"
version = "0.0.0"
edition = "2021"

[lib]
test = false
doctest = false
bench = false

[dependencies]
battle_state = { path = "../battle_state" }
core_data = { path = "../core_data" }
ability_data = { path = "../ability_data" }

chumsky = "0.10"